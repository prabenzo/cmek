# Time log

Working minutes per lane, per milestone (docs/plan/README.md › Working asynchronously). Planning time before the build clock is not counted (HANDOFF). [C] = Claude in the session, [B] = Ben on his laptop.

| Milestone | Planned | Actual [C] | Actual [B] | Started | Ended | Notes (cuts, fallbacks, decisions, parameter changes) |
| --- | --- | --- | --- | --- | --- | --- |
| M0 Skeleton and deploy | 20 | | | 7:39pm | 7:59pm | Live on Cloud Run; ticker check A PASS (2026-09-22: ticks_advanced=122, max gap 0.61 s): CPU flows while a stream is open; B (idle throttling) could not be measured: the viewer count never returned to 0 on Cloud Run after all clients closed (ARCH-NOTES 11); D9 fallback taken: --min-instances 1 --no-cpu-throttling; /health replaces /healthz (Cloud Run reserves /healthz); all three M0 decisions as recommended |
| M1 Data path | 45 | 29 | | 8:58pm | 9:27pm | Claude wrote everything (Ben reviews the four tests at his pickup); commits af38c48 `M1: data path`, 3e4a6b5 (generator fix). Measurement on /dev/shm, 60 s at 1,500/s: insert_mean_us 64 (rule ≤ 300 → no fallback, level 0: synchronous=NORMAL, wal_autocheckpoint 4000), insert_max_us 19,769 (WAL checkpoints; informational), ticks 121, delivered 609/s (capacity 640), internal 0, by_state [1000,0,0,0], backlog 50,607 rows (86 MB); S1 `grep -ac PLAINTEXT-CANARY` on the .db and -wal: 0 and 0. 300/s for 20 s (fresh process): accepted = delivered = 294/s, backlog 0, one generate per tenant (kms ok front-loaded). curl: 202 `{"id":87588,"tenant":"t-0042"}`, 404 unknown_tenant, 400 without X-Tenant-ID. Deviations: generator moved to an absolute arrival schedule (sleep-then-send gave 1,455/s at 1,500 offered; 1,508/s after, 20 s run); `Reclaim` built (not deferred); tenant-detail endpoint built minimally (not cut; M4 fills the lease fields); `Store.Dead` decrements total/backlogged; `metrics.OfferedSource` added; import-check regex gains the dot (go1.26 `entropy/v1.0.0`); `.gitignore` for the binary. `TestTwoWorlds` green under -race. `BenchmarkInsert` ns/op on Ben's laptop: pending |
| M2 CMEK core | 45 | 19 | | 10:04pm | 10:23pm | Claude wrote everything (Ben reviews at his pickup); commits 11f5d99 (core + kms), a5d01cf (tests + wiring), then the outcome commit. Per-tenant mutexes as decided (lock order `sched.mu` → `t.mu` → `reg.mu`); cut not taken (`TenantInflight = 2`, the errBusy row is live). Acceptance (300/s, tmpfs): row 1 revoke rank 3 (t-0754): purple in 7.6 s, `ingest_ps.key_revoked` 2/s, `403 {"error":"key_revoked"}` without Retry-After, `kms_ps.deny` 2 on one tick in ten, exactly one `REVOKED, 1 DEKs purged` line; row 2 restore: all ACTIVE in 4.6 s; row 3 gcp fast-fail: `[935,48,17,0]` at 5 s, `[815,105,80,0]` at 15 s, `[720,0,280,0]` at 30 s, `key_unavailable` 62–82/s late in the outage, `kms_ps.transient` peak 98/s settling 44–62/s, 0 invariant violations over 70 frames, timeline lines per transition; row 4 clear: all ACTIVE in 12.2 s, backlog ≈ 4 throughout (rejected gcp traffic never queued). Tests: `TestClassify`, `TestLease`, `TestManagerWalk` (11 rows) green under `-race`; the walk's row 11 found one bug (a deny zeroed the lease TTLs) before any live run. Deviations: cmek is 730 code lines / 887 with comments against the ≤ 600 bound (envelope move-out would not reach it; Ben decides); `internal/logx` allowed into the core (import regex amended); `Info` fills every field; scheduler/workers unchanged (M1 already had the branches); M2's wiring rows shrank accordingly. Nothing carried to M3-start. Review (PR #6, 2026-09-22, +12 working minutes): `probing` cleared after `sf.Do` returns (a probe that joined a finished flight left it set forever); `renew` rechecks the need inside the flight (concurrent cold callers generated two DEKs); KMS failures logged (throttled); fault body p50/p99 rule; `Lease.Until`; `fmtPurged` everywhere; KEK ids from the specs; `TestManagerExtras` (the M5 walk extras, pulled in) and `TestConcurrentColdCallers` (real goroutines) |
| M3 Admission and surges | 30 | 32 | | 10:50pm | 11:08pm | Claude wrote everything; commits 930482b (code) + the outcome commit. **Cut not taken**: the T+20 scan on run 1 (×10 from a 300/s baseline, tmpfs) reads PASS: `backlog.total ≥ 20,000` first at t+17 s and `overloaded` on every tick from then (68 ticks) with `l4.rejected_within_share_ps == 0` throughout, max backlog 22,296 (≤ 2× cap); rank 900 → 202 at t+5/30/44 s; rank 1 → `429 backlog_full` at t+5 s (its bucket held a token at that instant; the tile read `rate_limited` 408/s and `backlog_full` 614/s at t+10 s); ranks 20 and 30 → `503 overloaded` after the cap; `delivered_ps` 596 at 45 s (min 588 after t+10 s: no early dip on the tmpfs, M5's `L4CapacityFactor` input); `kms_ps.ok` 24–74/s per tick during ×10 (mean ≈ 50, a 124/s peak in the first seconds from cold tail tenants' first fetches); `internal` 0 at every tick; drain 22,296 → 4 in 70 s at ×1. Run 2 (two-class ring, `SchedLightTurns = 32`, fresh restart): delivered 618 at 45 s (min 576, mean 608 over t+10–45 s), within-share violations 0, drain to 3. Scenario curls: tenant_surge 200 then 409, `rate_limited` 704/s within 3 s (within share by design), accepted 346, `ends_at` ≈ t+60 s; stop → 200, next tick 0 and idle; idle stop 200; global_surge at 10 s: accepted 1,056 (the plan guessed ≥ 1,200; rank 1 is capped by then), delivered 608, backlog 4,665 rising; start/stop/start 200; unknown scenario 404; ×0 and unknown tenant 400/404; offered back to 300. Stop handler shipped (no fallback). Deviations: `Expire` built count-then-delete like `Reclaim` (+45, compiled, not wired); the sink now delivers a payload without a canary and counts a mismatch only when the canary names another tenant, so S2 stays evidence of cross-tenant delivery (three curl rows with `{}` bodies had been dead-lettered in run 1; M5 note), `TestSinkCanary` added; the `Dead` ledger line and the rank log were M1's already. Review (PR #7, 2026-09-22, +14 working minutes): the runner's child context is released on every exit path and `World.Stop` marks the runner stopping before cancelling (no `wg.Add` after `Wait`); a scenario's first phase is entered and its card published before `StartScenario` returns, and the slot frees before the card clears; `StopScenario` returns the cancelled name and the handler echoes it; `NewScheduler` clamps a nil share or `LightTurns < 1` to single-class; `overWithin` typo; tests added: `TestGate` (idle, share, cap-before-bucket, bucket, cut without fair share), `TestTwoClassNext` (light/heavy sequence, fall-through, double miss, off variants), `TestExpire`, `TestScenarioRunner` |
| M4 UI and scenarios | 55 | 35 | | 11:37pm | 12:12am | Claude wrote everything; commits 669ebf8 (metrics + scenarios), 2dea93c (page), 67471d2 (chart/timeline fixes), then the outcome commit. **No cut taken**: all four charts and hover/pin shipped (the box was bounded by observation wall clock, not writing; Ben's laptop numbers arrive by dictation and are added here). Verified in the session with headless Chromium (screenshots in the session's scratchpad: grid, tiles, cards with countdown, four charts drawing, timeline aggregating, hover tooltip, pinned strip with 8 audit lines, no console errors) and numerically from the stream on /dev/shm at 300/s. Blip: 333 gcp cells ringed, 28→59 yellow and 26→39 orange by 8 s (the World was 12 s old: few leases were soft-due yet; a warm World shows more), `key_unavailable` ≤ 6/s, healthy p99 8–12 ms, band all green in 11.5 s, `gcp restored: 333 tenants ACTIVE in 11.5 s, backlog drained in 0.5 s`; Stop on a fresh blip: next tick 0 ringed cells, `backlog.affected` 0. Outage: yellow 90 + orange 106 by 15 s (the band's hot/cold mix; ARCH's ≈ 333 is the ceiling), 276 orange by 30 s and 321 by 60 s, accepted 190–212/s, `key_unavailable` 56–74/s, `transient` 44–60/s after the 10 s peak, aws/azure untouched, healthy p99 4.6–7.3 ms flat, `gcp restored: 333 tenants ACTIVE in 12.0 s, backlog drained in 0.0 s` (parked rows are few on tmpfs). Revocation (rank 3 = t-0754): purple in 6.6 s / 10.7 s (two runs), `403 key_revoked`, `key_revoked` ≈ 20/s, exactly one `REVOKED, 1 DEKs purged` line, `GET /v1/tenants/t-0754` → `REVOKED`, `affected: true`, 6 audit entries; a restore of another tenant keeps the run open (204, card still `revoked`); Restore → green in 3.1 s, `recovery_s 2.8`, `drain_s 0.3`, `t-0754 restored: 1 tenants ACTIVE in 2.8 s, backlog drained in 0.3 s`. Slow KMS: `inflight.azure` 5–12 with aws/gcp 0 (the must-see ≥ 5 and ≥ 5×: PASS; the M2 note's "pins at 32" is superseded, the card never claims it), 6–16 yellow and 2–10 orange azure cells, `transient` 4–20/s, `key_unavailable` ≤ 6/s, healthy p99 7–13 ms flat once the ×10 drain had ended (the run started 40 s after ×1, with 5,294 rows still draining: the first 20 s of its p99 and its frozen baseline were the drain's, not the fault's; observe again in the browser), `azure restored: 333 tenants ACTIVE in 5.3 s, backlog drained in 0.3 s`. Tenant surge (rank 5 = t-0108): `rate_limited` 656–684/s from the first tick, all **within share** (`l4.rejected_over_share_ps` 0, as the card says), accepted ≈ 400/s, delivered ≈ 400/s, `offered_ps` 801.6, `kms_calls_per_min` 4 for the tenant, `kms_ps.ok` 34–50/s overall (flat), ringed cell 1; healthy p99 read 62 ms at +2 s and +5 s (baseline ≈ 7 ms) then 13 ms at +10 s and 9 ms at +20 s: a ≈ 5 s spike at surge start that would trip a 1.25 × max(baseline, 25 ms) L1 for ≈ 3 s after `L1Grace` — **for M5's L1 decision** (widen `L1Grace` to 5 s, or accept a brief red); Stop → `rate_limited` 0 on the next tick, offered back to 300. ×10 plateau (20 s): `delivered_ps` 608–630, **mean ≈ 620** → M5 `Capacity` 620 (L4 threshold 558/s); `backlog.total` reached the 20,000 cap at 20 s (run 1: 20,370); healthy p99 3–16 s under ×10 (everyone is backlogged; not a scenario). Healthy p99 **baseline on this box: 5–8 ms** (< 25 ms, so `L1Floor` is what L1 compares against); Cloud Run baseline pending Ben's deploy (step 8 fallback: read in M5's first minutes). Timeline: per-second flush of every transition, 31 lines in the outage's first 10 s, 137 over the outage's 80 s, **199 over slow KMS's 80 s** (cold azure cells' single `→ KEY_UNAVAILABLE` / `→ ACTIVE` lines dominate; ARCH's ≈ 60 was the ride-through churn only): the ring (200) is not overrun but the pane scrolls; the tuning lever is `TimelineAggregateMin` 3 → 2 (Ben's call at his pickup, the one `Demo()` iteration; not taken by Claude). Deviations: p99 interpolation uses the sample's mid position `(rank − cum − ½)/in` so the result stays inside `[lo, hi)` (the plan's `(rank − cum)/in` returned the upper edge when every sample sat in one bucket; `TestHistP99` pins it, with 1,000 samples spread over 10–30 ms shifted 10 % → ratio < 1.25); buffered transitions flush once per second (the plan said per tick: at 2 Hz the outage posted 2–3 lines per tick); `StartScenario` enters the first phase and publishes the card before returning (PR #7's runner test); the tail publishes phase `recovery` with `ScenarioTailMin` as its countdown; `Recovered` marks are cleared with the card at the end (M5 reads them during the run). 0 server errors across both runs (≈ 12 min). Ben's browser pass (blip, revocation + Restore, tenant surge on chart 2, slow KMS on chart 2, Cloud Run baseline + live revocation) pending at his pickup. Browser notes (PR #10, 2026-09-23, +6 working minutes): the invariant lights are named (spec short names, full statement and live check on hover) and the legend swatches describe each state on hover. Review (PR #10, +12 working minutes): two should-fix (idempotent `Revoke`/`Restore` so a revocation run leaves one enable event in the ground truth; the named lights, already pushed) and four nits taken (`TestTransitionLines` + flush-instant timestamps, `reconnect` listener dropped until M5, open-ended recovery phase with "waiting for drain", pinned strip via `textContent` with snake_case audit entries and `latency_ms`). Ben still saw no hover text (+5 working minutes): the static files had no cache validators, so his browser kept the previous page; they are now served `Cache-Control: no-cache`, and the hover text shows at once in the page's own box on mouseenter instead of the native title tooltip |
| Tink adoption (side branch `claude/tink-go`) | 40 | 14 | | 12:30am | 12:44am | Claude wrote everything after Ben's PR #8 approval ("Plan LGTM, please proceed", 2026-09-23); commits d2628fd (code + tests) and the docs commit. Round trip as Ben decided: `keyset.WriteWithContext`/`ReadWithContext` plus `kms.CodeFromText`; `insecurecleartextkeyset` nowhere in the dependency graph. Spike facts (scratchpad, deleted): wrapped keyset 142 bytes; Tink's read error text `keyset.Handle: decryption failed: <cause>`, write `keyset.Handle: keyset.Handle: encryption failed: <cause>`, a wrong KEK `aead_factory: decryption failed` (the gate maps it to `AccessDenied`). Acceptance (300/s, tmpfs, one run): baseline delivered 294/s, healthy p99 7.3 ms, kms 42/s, `internal` 0; blip: `[858,93,49,0]` at 9 s, `gcp restored: 333 tenants ACTIVE in 11.1 s, backlog drained in 0.0 s`; revocation (rank 3 = t-0754): purple in 9.6 s, `403`, `key_revoked` 22/s, exactly one `REVOKED, 1 DEKs purged` line, the tenant's audit shows `unwrap denied: keyset.Handle: decryption failed: kms azure: KeyDisabled: kek-t-0754 is disabled` (the code survives Tink), Restore → green in 4.6 s, `t-0754 restored: 1 tenants ACTIVE in 4.1 s, backlog drained in 0.1 s`; ×10 plateau 606–630 delivered/s (≈ 620, unchanged), backlog cap 20,393 at 20 s, `insert_mean_us` 59 (M1: 64). Service log: 13 ERROR lines in 146 s, all the throttled expected ones (9 `kms call failed … Unavailable: injected fault` during the blip, 2 `kms call denied … KeyDisabled`, 1 `batch released: key revoked`, 1 encryption-side `injected fault`), 0 unexpected. Tests: `TestGate`, `TestCodeFromText`, `TestCodeThroughTink`, `TestWrappedDEK`, `TestNoPlaintextAtRest`, the extended `TestEnvelope` and `TestClassify`; everything green under `-race`; import regex, globals grep, gofmt, vet clean. Deviations: `internal/cmek` is 789 code lines (same rule as M2's 730 measured 774 on the base): the two fetcher helpers, the classify fallback and the nil-primitive guards outweigh the 32 lines the envelope lost, so the plan's "≈ 40 fewer" did not happen; `Handle.Zero` is a reference drop (Tink does not zeroize); `TestNoPlaintextAtRest` lives in `world_test.go`; the fake gate maps a non-verifying blob to `AccessDenied` as `Unwrap` did; one code commit instead of two pushes (the interface and its callers do not compile apart). Review (PR #9, +5 working minutes): no blocking findings; three nits taken: `kms.Cause` trims the audit Detail to the `kms …` frame, `CodeFromText` anchors on the first frame, the fake call order in DESIGN-REFERENCE drops the truth-log steps |
| No key cache (sixth card, before M5) | 40 | 32 | | 1:55am | 2:27am | Claude wrote everything after Ben's approval in chat ("implement it", 2026-09-23); commits 85f829c (pass-through, two scenarios, card), ff77495 (peak readings, copy from the first run), then the docs commit. Two acceptance runs at 300/s on tmpfs, both alike. **One band + blip (55 s)**: KMS calls 160–204/s (baseline 30–32; gcp peaked at 202/s vs aws 26/s), in flight gcp 2–6, affected p99 78–108 ms against a 7–10 ms baseline, **healthy p99 17–44 ms** (the shared eight-worker pool: gcp deliveries each hold a worker for a KMS round trip; it fell back to 3–10 ms during the blip when gcp deliveries stopped), so L1 will read red or flickering for the cached two thirds too: the card says so, a deviation from the plan's expectation; the blip parked 187 / 206 gcp tenants (each on its first event, 29 → 187 over the 10 s; low-rank gcp tenants send less than once per 10 s), `key_unavailable` 58–108/s, `transient` 50–114/s, all recovered ≤ 5 s after the clear, `gcp restored: 333 tenants ACTIVE in 0.1 s, backlog drained in 0.1 s`. **Everyone + surge (90 s)**: lead 15 s at 284–330/s accepted: delivered 188–226/s (capacity 640), KMS 488–534/s, backlog 358 → 1,315, end-to-end p99 already 1.4 → 3.3 s; surge: accepted 1,036–1,134/s until the cap (≈ 30 s in), then `overloaded` 1,158–1,268/s, delivered 196–242/s throughout, KMS peak 1,238 / 1,344/s then ≈ 500/s (accepted + delivered), in flight peak aws 18 gcp 23 azure 17 (never the 32 bulkhead: admission sheds first, so `transient` 0 and no orange cells, a deviation from the plan's "timeouts park tenants"), backlog peak 23,581 / 23,310 (cap 20,000 + in-flight rows), e2e p99 peak 56–57 s (queue wait; healthy 34–37 s), L1, L2 and L4 red together; recovery 0.5 s, drain 57.1–57.5 s at ≈ 600/s with the cache back (inside `ScenarioTailMax`), `top 150 restored: 150 tenants ACTIVE in 0.5 s, backlog drained in 57.5 s`. A plain `provider_blip` afterwards reads as in M4 (`gcp restored … in 9.5 s / 11.0 s`): the cache is back. `internal` 0 both runs; service log 43 ERROR lines per run, all the expected throttled ones from the blip (`kms call failed`, `batch released`). Tests: `TestPassThrough` (12 rows), `TestNoCacheScenarios` (both runs), `TestProviderCallsPS` (+ peaks); everything green under `-race`; gofmt, vet clean; headless: the card renders with its two starts, a start disables every other start and shows `cache_off · 29 s`, Stop clears it. Deviations: summary lines report per-phase peaks (`metrics.ResetPeaks`/`Peaks`) because the last tick of the surge under-read KMS calls three times; the latency lever (10 ms) was not taken because it would erase the global run's lead-phase story (capacity ≈ 314/s at 10 ms); the acceptance script's phase polling lagged 10–15 s behind the stream (each sample greps the growing SSE log), so the blip timeline was read from the stream log instead. Review (PR #11, +8 working minutes): no blocking findings; three nits taken (generate through singleflight in pass-through, summary lines only on a natural end, the in-flight cap named in the copy) |
| M5 Checker and hardening | 25 | 38 | | 2:59am | 3:37am | Claude wrote everything after Ben's approval in chat ("M5 plan LGTM, start the implmentation", 2026-09-23; every decision as recommended, six lights). Commits: the M5 commit (checker, sink ring, census, holder, reset, SSE deadline and reconnect, expire in the sweep, script, README), then the docs commit. Suite green under -race (nine packages; `TestCheckerTurnsRed`, `TestWindows`, `TestL1Persistence`, `TestHolderIdleRule`, `TestFastFailStormRace` new). Lifecycle curl on a second port: stream → `w-1`, `POST /v1/reset` → `w-2`, a stream 12 s after the last viewer left → a fresh id, one within 10 s → the same; old database files deleted, `ingest_ps.internal` 0, six green lights with `at` advancing. **Every-scenario run 1** (scratch disk, this box, four vCPUs shared with a second binary for part of it): blip and revocation PASS; slow_kms L1 red 4 s (healthy p99 32–34 ms against the 31 ms limit while a second binary took traffic on the same box); tenant_surge L1 red 5 s at the surge start (healthy p99 67 ms from the 200-event token-bucket burst; M4's row had predicted it) and 1 s at +30 s (32 ms); **global_surge L1 red for the whole run: healthy p99 1.3–4.7 s** (every tenant sends 5×; the 850 lighter tenants queue behind capacity like everyone else; M4's tuning table: "restrict L1 to non-surge scenarios") and L4 red 2 s at the start (`delivered 451/s < 558`: the 5 s delivered window still held pre-surge seconds); no_cache and provider_outage failed only the script's own S1 freshness bound (2 s against a 5 s scan period); no_cache_surge PASS (L1 294 and L4 185 red frames, both allowed then, green after the tail). Fixes: L1 not judged in the two global surges (`global surge` detail), `L1Grace` 2 → 7 s, `L1For` 3 s (new), `L4Settle` 2 → 7 s, the script's S1 bound 7 s and no_cache_surge's allowed reds L4 only. **Every-scenario run 2** (the pushed binary, alone on the box): all eight PASS, exit 0: provider_blip 54 frames, key_revocation 120, slow_kms 154, tenant_surge 154 (no L1 red: the opening burst is inside the 7 s grace), no_cache 160 (L1 red 20 frames, allowed, green after the tail), provider_outage 180, global_surge 299 (L1 `global surge`, L4 green throughout: delivered 516–634/s, median 596, against the 558 threshold, backlog capped at 20,000, no within-share tenant shed, healthy p99 3.2–4.6 s on the tile), no_cache_surge 379 (L4 red 175 frames, allowed, green after the tail); every light fresh at the end of every run; `ingest_ps.internal` 0 throughout. Resting healthy p99 on this box's disk ≈ 18–22 ms (M4 measured 7–13 ms on /dev/shm): on /dev/shm the same binary rests at 4–11 ms (median 9), so the disk's WAL writes add ≈ 10 ms at the tail and the 31 ms L1 limit has half the headroom here that it has on tmpfs or Cloud Run's in-memory filesystem. |
| Page tweaks after M5 (shorter no-cache card, hide cards, hide one card) | — | 18 | | 3:40am | 3:58am | Ben's asks in chat; commits 6575011, f8d8ef2, a65a0eb; each verified in headless Chromium |
| M6 Rationale | 45 | 5 | | 3:58am | 4:03am | Claude's part is the README only (Ben, 2026-09-23: "just write the readme file, I will handle the rest manually"): scenario guide, stale-OK sentence, Rationale, AI transcripts (ownership `wc -l internal/cmek/*.go` excluding tests = 1,094 lines, 887 code), Time spent; commit b097c1e. RATIONALE.md, the video, the transcripts and their index, the deploy and this row's [B] minutes are Ben's, by hand |
| Buffer | 20 | | | | | |
| **Total** | **285 (4:45)** | **222 (3:42)** | **20 (M0) + Ben's M6 lane** | 7:39pm | | [C] is the sum of the Claude rows above (M0 was Ben's deploy lane); the Tink, no-cache and page-tweak rows were not in the plan's 285 |
